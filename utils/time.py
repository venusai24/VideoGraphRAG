"""Time conversion placeholders for clip timestamps and spans."""

from __future__ import annotations


def to_seconds(timestamp: str) -> float:
    """Convert a timestamp string to seconds (placeholder)."""
    _ = timestamp
    return 0.0
